{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build chrome",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "command": "pnpm",
      "args": ["build:chrome"],
      "type": "shell"
    }
  ]
}
